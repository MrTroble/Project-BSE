#include "NifLoader.hpp"
#include <NifFile.hpp>
#include <TGEngine.hpp>

namespace tge::nif {

	size_t load(const std::string& name, void* shaderPipe) {
		const auto api = tge::main::getAPILayer();

		return -1;
	}
}